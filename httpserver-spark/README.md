# A relatively small HTTP-server written in Java (using Spark)
* The server serves an endpoint (/headers) which returns the client request headers.

## Usage:
1. Compile and create Docker image
    - make build
